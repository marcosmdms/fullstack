## Objetivo: Criar um servidor e deixá-lo operacional

* case sensitive e camelCase

## tipos de importação
*comomjs*
module.exports e require
-----------------------------
Import {} from 
Export
----------------------------
JSON.stringify({status: "ok"})
node --watch server.ts (para nao ter que ficar iniciando o servidor contantemente)

---------------
**foi criada uma api simples**

import {createServer} from 'node:http'; // cria servidor
createServer(function (request, response){

console.log("toc toc ");

request.writeHead(200, { "content-type":"aplication/json"});
response.end(JSON.stringify({status: "ok"}));

}).listen(3000);

**-------------------------------------------**

## Uma organização simples e saudável seria:
------------------------------------------------
meu-projeto/
│
├── index.html
├── server.ts          ← ponto de entrada
│
├── src/
│   ├── app.ts         ← lógica da aplicação
│   ├── services/      ← regras de negócio
│   ├── controllers/   ← controle das requisições
│   └── utils/         ← funções auxiliares
│
├── package.json
└── tsconfig.json
-------------------------------------------------











### npx tsx server.ts